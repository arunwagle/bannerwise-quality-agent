"""Services package — API-driven data access layer.

Currently backed by mock data. Replace individual services
with real implementations when ready:
  - mock_router_service  → Model Serving endpoint call
  - mock_corpus_service  → Delta table SQL query
  - mock_history_service → Delta table SQL query
"""
